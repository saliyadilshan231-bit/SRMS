import LibraryCoursesScreen from '@/screens/LibraryCoursesScreen';

/** Module grid & search — after library welcome */
export default function LibraryBrowseRoute() {
  return <LibraryCoursesScreen />;
}
