import PeerTutorRegistrationScreen from '@/screens/PeerTutorRegistrationScreen';

/** Tutor signup / application — same screen as peer-tutor-register (preferred teaching modules, etc.). */
export default function PeerTutorSignupRoute() {
  return <PeerTutorRegistrationScreen />;
}
